// const numbers$ = of(1, 2, 3, 4, 5);

// const tripleNumber = number => number * 3;
// const isEvenNumber = number => number % 2 === 0;

// const pipedTransform$ = numbers$.pipe(
//   map(tripleNumber),
//   filter(isEvenNumber)
// ).subscribe(number => console.log('PIPED: ', number));
